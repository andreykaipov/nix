
## Layout
```
.
├── dock               # MacOS dock configuration
├── casks.nix          # List of homebrew casks
├── default.nix        # Defines module, system-level config
├── home-manager.nix   # Defines user account, dock, and homebrew casks
├── homebrew.nix       # nix-homebrew tap configuration
├── secrets.nix        # agenix secrets
```
