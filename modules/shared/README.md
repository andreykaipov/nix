## Shared
Shared nixpkgs configuration (overlays, unfree/broken settings) imported by system modules (darwin, nixos).

Home-manager configuration lives separately in `modules/home/`.

## Layout
```
.
├── default.nix        # nixpkgs config and overlay imports
```
