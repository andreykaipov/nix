# vim: filetype=gitconfig

[user]
    name = Andrey Kaipov
    email = andreykaipov@users.noreply.github.com

    # TODO: use the same name on every machine and add each to github
    signingkey = ~/.config/ssh/keys/dustbox.pem.pub

[gpg]
    format = ssh

[credential]
    helper = cache --timeout=10000

[diff "plist"]
    textconv = plutil -convert xml1 -o -

[includeIf "gitdir:~/projects/work/**"]
    path = work

[includeIf "gitdir:~/projects/work/github/**"]
    path = github
